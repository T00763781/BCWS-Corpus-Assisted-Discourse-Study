<UserControl x:Class="OpenFireside.Desktop.Views.IncidentDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Bold"/>
        <Border Margin="0,16,0,0" Padding="16" Background="#102039" CornerRadius="14">
            <StackPanel>
                <TextBlock Text="{Binding IncidentNumber}" FontSize="18" FontWeight="SemiBold"/>
                <TextBlock Text="Incident detail is scaffolded as its own workspace so future list/dashboard/map interactions can open it in a detached window." Margin="0,8,0,0" TextWrapping="Wrap" Foreground="#EAF2FF"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
