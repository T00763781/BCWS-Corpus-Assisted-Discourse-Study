# Codex Handoff

Use this package as the canonical Open Fireside shell-first repo.

Rules:
1. Preserve the shell aesthetic and route structure.
2. Keep only factual endpoint-backed views live.
3. `Dashboard`, `Incidents`, and `Configure > Sources` may stay wired because they are backed by verified BCWS endpoints.
4. `Weather`, `Maps`, and `Discourse` should remain blank until they have verified sources.
5. Stubbed dashboard panels are limited to `Discourse Signals` and `Incidents (pinned)`.
6. Do not introduce prose, PM framing, validation philosophy, inferred product meaning, fabricated summaries, or placeholder repopulation.
